<!DOCTYPE html>
html(lang="en")
    head
        include partials/headers
    body.login
        .contenedor-formulario
            h1= nombrePagina

            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p=error

            p Agrega tu e-mail para restablecer la contraseña
            form.caja-login(method="POST" action="/reestablecer")
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" placeholder="Tu email")
                .campo.enviar
                    input.boton(type="submit" value="Reestablacer contraseña")
                .campo.acciones
                    a(href="/iniciar-sesion") Ya la recorde, volver a login