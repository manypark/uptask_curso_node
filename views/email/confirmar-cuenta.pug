<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body
        h2 Confirmacion de cuenta UpTask
        p Ya estas casi inscrito, solo presiona el siguiente boton
        a(href=`${confimarUrl}`) Confirmar cuenta
        p Si no puedes acceder a este enlace, visita #{confimarUrl}
        p Si no solicitaste este e-mail, hacer caso omiso
